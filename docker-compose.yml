services:
  dashboard:
    build:
      context: .
      target: deps
    command: bun run dev:docker
    ports:
      - "5173:5173"
    environment:
      BACKEND_URL: ${BACKEND_URL:-http://host.docker.internal:8090}
    volumes:
      - .:/app
      - /app/node_modules
